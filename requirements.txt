# ──────────────────────────────────────────
# MindLog — Python Dependencies
# ──────────────────────────────────────────

# Web app UI framework (chat interface, page routing)
streamlit>=1.30.0

# OpenAI API client (conversation, extraction, summarization)
openai>=1.30.0

# Load API keys from .env file
python-dotenv>=1.0.0

# Pretty-print evaluation result tables (Table 1)
tabulate>=0.9.0

# Schema validation for LLM extraction output (affect, energy, etc.)
pydantic>=2.0.0